# som-core — Kohonen SOM training library

Reusable Python package for Self-Organising Map training, CSV loading, and plot helpers.

```bash
pip install -e packages/som_core
```

```python
from som_core import SOM, train_som_from_csv
```
